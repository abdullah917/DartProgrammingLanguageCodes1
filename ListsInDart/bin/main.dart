import 'dart:io';

void main() {
  var scores = [];
  scores.add(10);
  scores.add(20);
  scores.add(30);
  scores.add(40);
  scores.add(50);
  for (int i = 0; i <= scores.length; i++) {
    print(scores);
  }
}
